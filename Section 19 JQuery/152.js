// Selecting Elements with jQuery

document.querySelector("h1");

// But using jquery
$("h1");

// we can select multiple elements using only one $
// i.e jusing javascript
document.querySelectorAll("button");
// but using jquery
$("button");